# slot_substitution.v1

The draft below was produced by deterministic code that replaced one slot value.
Rewrite only as needed for natural grammar.

Style contract: {{STYLE_GUIDE}}

Hard constraints:

- The draft `intent` and complete `slots` list are ground truth.
- Copy every slot type and literal value exactly.
- Every slot value must occur in `utt`.
- Do not restore the old value, add slots, or remove slots.
- Use Traditional Chinese appropriate for Taiwan.

Procedural draft JSON: {{DRAFT_JSON}}
